import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  img: {
    width: '100%',
    aspectRatio: 200,
    marginRight: 10,
    paddingTop: 10,
  },
});

export default styles;
